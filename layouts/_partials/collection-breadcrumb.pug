ul.breadcrumb
  li
    a(href=site.url)= site.title

  if page.pagination
    li
      - var collection = find(collections, {'name': page.collection})
      a(href=url_for(collection.name))= collection.title

    li page #{page.num} of #{page.pagination.totalPages}
  else
    li= page.title
